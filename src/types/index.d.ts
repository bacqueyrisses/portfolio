export type TechnologyProps = {
  technology: {
    id: number;
    name: string;
    link: string;
  };
};

export type ProjectProps = {
  project: {
    id: number;
    name: string;
    description: string;
    stack: string;
    link: string;
  };
};
